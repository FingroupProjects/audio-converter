version: '3.8'

services:
  audio-converter:
    build: .
    container_name: audio-converter
    ports:
      - "8000:8000"
    volumes:
      - ./output:/app/output
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1

